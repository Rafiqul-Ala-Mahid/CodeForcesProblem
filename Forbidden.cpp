#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        /* code */
        int n, m, d;
        cin >> n >> m >> d;
        int p[n];
        for (int i = 0; i < n;i++)
            cin >> p[i];
        int a[m];
        for (int i = 0; i < m;i++)
            cin >> a[i];
        int s = 0,b[m];
        for (int i = 0; i < m; i++){
            
        }
    }
}